import type { MailMessage } from "./types"; const encoder = new TextEncoder(); export function buildMime(message: MailMessage): ArrayBuffer { const body = message.html ? `Content-Type: multipart/alternative; boundary="mail-boundary"\r\n\r\n--mail-boundary\r\nContent-Type: text/plain; charset=utf-8\r\n\r\n${message.text ?? ""}\r\n--mail-boundary\r\nContent-Type: text/html; charset=utf-8\r\n\r\n${message.html}\r\n--mail-boundary--` : `Content-Type: text/plain; charset=utf-8\r\n\r\n${message.text ?? ""}`, bytes = encoder.encode(`From: ${message.from}\r\nTo: ${message.to.join(", ")}\r\nSubject: ${message.subject}\r\nMIME-Version: 1.0\r\n${body}`); return bytes.buffer.slice(bytes.byteOffset, bytes.byteOffset + bytes.byteLength) as ArrayBuffer; }
